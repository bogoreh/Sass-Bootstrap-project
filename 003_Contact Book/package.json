{
  "name": "contact-book",
  "version": "1.0.0",
  "description": "A responsive contact book with SASS and Bootstrap",
  "scripts": {
    "sass:watch": "sass --watch scss:css",
    "sass:build": "sass scss/style.scss css/style.css --style=compressed",
    "dev": "npm run sass:watch"
  },
  "devDependencies": {
    "sass": "^1.66.1"
  }
}